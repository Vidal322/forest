# Server Configuration
SERVER_PORT=8080

# Database Configuration
# DB runs in Docker; app runs locally — host is localhost, port is exposed by docker-compose
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=forest_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=forest_db
POSTGRES_VOLUME_NAME=forest_db_volume
ADMINER_PORT=8000
